"use client";

import { GlassFilter } from "@/components/ui/liquid-glass";

/** Mounts the shared SVG filter once for all v1 liquid-glass surfaces. */
export function GlassRoot() {
  return <GlassFilter />;
}
